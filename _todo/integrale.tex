\input{../../common/exo_begin.tex}%


\begin{xexempleprog2}{Calcul d'une intégrale avec la méthode des rectangles}{exemple_inte_cor}\label{exemple_inte}
\indexfr{intégrale}\indexfr{méthode des rectangles}

\subsectionexemple{Description de l'objectif}

On cherche à calculer une intégrale en utilisant la méthode des rectangles (voir figure~\ref{exemple_integral}).
%
				\begin{figure}[ht]
				\begin{center}\begin{tabular}{|c|}\hline
    		\includegraphics[height=6cm, width=7cm]{\filext{../python_cours_exemple/image/int}} \\ \hline
    		\end{tabular}\end{center}
    		\caption{	Illustration du calcul approché d'une intégrale à l'aide de la méthode des rectangles.}
    		\label{exemple_integral}
    		\end{figure}
%
L'intervalle de l'intégrale est noté $\cro{a,b}$ et la fonction à intégrer $f$. On divise cet intervalle en $n$ petits segments et on fait la somme des aires des petits rectangles délimités par l'axe des abscisses et la courbe de la fonction~$f$.
%
			\begin{eqnarray}
			\int_a^b f(x) dx &\approx& \frac{b-a}{n} \; \summy{i=1}{n}  f\pa{ a + i \frac{b-a}{n} }
			\end{eqnarray}
%
On pourra prendre par exemple~:
%
			\begin{eqnarray}&&\begin{array}{ll}
			a 	& = -2 \\ b & = 3 \\ f(x) & = x cos (x) \\ n & = 20
			\end{array}\end{eqnarray}
%			
\end{xexempleprog2}



\begin{xexempleprog2cor}{exemple_inte}\label{exemple_inte_cor}
\indexfrr{fonction}{xrange}\indexfrr{fonction}{cos}\indexfrr{module}{math}

\subsectionexemple{Différents programmes}

Le calcul approché d'une intégrale est un calcul mathématique simple. Il ne repose que sur une boucle et celle-ci obéit à la règle que toute boucle doit être précédée d'une étape d'initialisation (ici, \codes{sum = 0}). C'est une étape qui n'apparaît pas souvent dans la phase de rédaction d'un algorithme, il n'y a pourtant pas d'execption à cette règle.

\inputcode{../python_cours_exemple/programme/integrale.py}{calcul d'une intégrale}

\indexfrr{module}{scipy}
Le module \codes{SciPy}\footnote{Le module SciPy propose de nombreux algorithmes, algorithmes de classification, intégration, optimisation, génération de nombres aléatoires, traitement du signal, interpolation, algorithmes génétiques, algèbre linéaire, transformée de Fourier, dessin de courbes 2D, 3D, adresse~: \httpstyle{http://www.scipy.org/}.} propose de nombreux algorithmes mathématiques tels que le calcul d'intégrales. Le programme précédent se résume alors à quelques lignes.
%
\indexexemples{intégrale, scipy}{}
\begin{verbatimx}
import scipy.integrate as integral             # on importe le module d'intégration
def fff(x): return x * math.cos (x)            # on définit la fonction à intégrer				
sum = integral.quad (fff, a,b, args=()) [0]    # on appelle la fonction d'intégration
print "intégrale avec scipy : ", sum           # on affiche le résultat
\end{verbatimx}
%
Le programme affiche les lignes suivantes~:
%
\begin{verbatimx}
intégrale :  -1.96640795695
intégrale avec scipy :  -1.96908048953
\end{verbatimx}
%
\end{xexempleprog2cor}


\input{../../common/exo_end.tex}%
